sudo: required
services:
  - docker

before_install:
  - docker build -t mohamedhamed1990/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run mohamedhamed1990/react-test npm test -- --coverage

after_success:
  - docker build -t mohamedhamed1990/multi-client ./client
  - docker build -t mohamedhamed1990/multi-nginx ./nginx
  - docker build -t mohamedhamed1990/multi-server ./server
  - docker build -t mohamedhamed1990/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push mohamedhamed1990/multi-client
  - docker push mohamedhamed1990/multi-nginx
  - docker push mohamedhamed1990/multi-server
  - docker push mohamedhamed1990/multi-worker